# Issue tracker

This repository uses GitHub Issues through the `gh` CLI. Issues created from `to-tickets` should use the `ready-for-agent` label when they satisfy the acceptance criteria and have no open blockers.

Pull requests are not part of the triage queue by default. Local ticket drafts may live under `.scratch/<feature>/issues/` until the GitHub repository exists.
